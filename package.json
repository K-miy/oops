{
  "name": "oops",
  "version": "0.1.0",
  "description": "Out of Parent's Shape — offline-first PWA",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "make build",
    "dev": "make dev",
    "test": "make test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
